{#
/**
 * @file
 * Theme override for the images field in highlight item paragraph.
 * This completely removes all Drupal field wrapper divs.
 */
#}
{%- for item in items -%}
  {{- item.content -}}
{%- endfor -%}