{#
/**
 * @file
 * Theme override for the pillars field in pillars section.
 * This completely removes all Drupal field wrapper divs to maintain proper grid structure.
 */
#}
{%- for item in items -%}
  {{- item.content -}}
{%- endfor -%}