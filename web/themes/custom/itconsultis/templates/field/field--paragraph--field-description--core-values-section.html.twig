{#
/**
 * @file
 * Theme override for the description field in core values section.
 * This removes the extra field wrapper divs.
 */
#}
{%- for item in items -%}
  {{- item.content -}}
{%- endfor -%}
