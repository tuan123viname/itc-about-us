{#
/**
 * @file
 * Theme override for the icon field in core value item paragraph.
 * This completely removes all Drupal field wrapper divs.
 */
#}
{%- for item in items -%}
  {{- item.content -}}
{%- endfor -%}
